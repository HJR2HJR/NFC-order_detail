# NFC Order Detail

## 本地启动

需要先安装 Node.js。

```bash
npm install
npm run dev
```

启动后打开：

```text
http://localhost:3000/
```

## 构建

```bash
npm run build
```

构建产物会生成在 `dist/` 目录。

## GitHub Pages

项目已适配 GitHub Pages 子路径部署。上传到 GitHub 后，可以使用 GitHub Pages 发布构建后的 `dist/` 内容。
